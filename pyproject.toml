[project]
name = "ovllib_dev"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ['InquirerPy', 'pydantic', 'tomlkit']

[project.scripts]
ovllib_dev = "main:main"
